{% extends 'base.html.twig' %}
{% block title %}Test accueil!{% endblock %}

{% block body %}
<div class="container">
   <div>
       <h3>Page d'accueil TEST</h3>
   </div>
    <div class="row">
        <div class="col3 m-2">
            <a class="btn-lg btn-primary" href="{{ path("student_form") }}">Formulaire "student"</a>
        </div>
        <div class="col3 m-2">
            <a class="btn-lg btn-success" href="{{ path("student_crud_index") }}">CRUD "Student"</a>
        </div>
        <div class="col3 m-2">
            <a class="btn-lg btn-success" href="{{ absolute_url("/api") }}">API & Docs</a>
        </div>
    </div>
</div>
{% endblock %}
